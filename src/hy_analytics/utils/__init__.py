"""Shared helpers: logging setup, datetime/tz utilities, config loading, etc.

Keep helpers small, pure, and reusable. Anything domain-specific belongs in the
relevant ``scrapers/`` or ``services/`` module instead.
"""
